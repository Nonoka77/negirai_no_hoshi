<% if user_signed_in? %>
    <% if current_user.already_calmed?(post) %>
        <%= link_to post_calms_path(post), method: :delete, remote: true do %>
            <%= icon('fas', 'mug-hot') %>
            <%= post.calms.count %>
        <% end %>
    <% else %>
        <%= link_to post_calms_path(post), method: :post, remote: true do %>
            <%= icon('fas', 'coffee', style: 'color: gray;') %>
            <%= post.calms.count %>
        <% end %>
    <% end %>
<% end %>
